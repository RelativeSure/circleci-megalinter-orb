description: >
  Run Megalinter on your project to lint all supported files.
  This will run without using a flavor.
usage:
  version: 2.1
  orbs:
    megalinter: relativesure/megalinter@2.0.0
  workflows:
    run-megalinter:
      jobs:
        - megalinter/run
